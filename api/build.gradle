plugins {
    id 'maven-publish'
}

version = '1.0.0'

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = 'july-safe-api'
            from components.java
        }
    }

    repositories {
        mavenLocal()
    }
}
